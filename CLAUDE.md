# CLAUDE.md

Instructions for coding agents in this repository live in `AGENTS.md`.
Read and follow it first: @AGENTS.md

`AGENTS.md` is the routing entry point: startup protocol, source-of-truth map,
safety and approval rules, setup/test commands, and the documentation-maintenance
contract. Its own maintenance rules live in `docs/agents/how-to-agents.md` (read
that before editing `AGENTS.md`; not imported here to keep session context small).

For code navigation (concept -> file -> test -> endpoint) start with
`docs/architecture/code-map.md`.
